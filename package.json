{
  "name": "huawei-cloud-smart-assistant",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "frontend:install": "cd frontend && npm install",
    "frontend:dev": "cd frontend && npm run dev",
    "frontend:build": "cd frontend && npm run build",
    "backend:dev": "cd backend && python -m uvicorn app:app --host 0.0.0.0 --port 8003 --reload",
    "backend:prod": "cd backend && python -m uvicorn app:app --host 0.0.0.0 --port 8003"
  }
}
